# 🟧 HTML Tag vs Element

## HTML Tag

A **tag** is the markup written inside angle brackets that defines the element name.

It **does not include attributes** — attributes are written inside the opening tag but are **not part of the tag itself**.

### Examples

```html
<p>
  <img />
</p>
```

## HTML Element

An **element** includes the **tag + content + attributes** (if any).

### Examples

```html
<p class="text">Hello</p>

<img src="image.jpg" alt="profile image" />
```

## Key Difference

- **Tag** → Element name written inside `< >`
- **Element** → Complete structure created by the tag
